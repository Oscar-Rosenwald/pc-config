This is a repository of all my computer configurations, implemented for and by Nix.

To make it work, create a symlink =/etc/nixos= to =nix/= in a local copy of this
repository, e.g. =/home/user/config/nix=. Don't forget to move the original
hardware configuration file there first.
* TODOs
** TODO Sudoers
   using ~security.sudo~ (look it up).
** TODO Sway config
   Migrate from the i3 config files, i3 status bar, dunst, etc.
** TODO Create the =default= and =work= ssh keys
   Ideally do this in home manager automatically unless the files already exist.
** TODO Migrate Emacs
   Jesus Fucking Christ, that'll be a nightmare.
** TODO Transcribe jujustu home-manager
   TOML is a supported nix format. You can do anything the native way.
** TEST ~programs.bash.profileExtra~
   There's a TODO comment. Do we need to do these even on nix? Must test.
* COMMENT File config
#+TODO: TODO TEST DONE
# Local variables:
# End:
