This is a repository of all my computer configurations, implemented for and by Nix.

You will not find tools here, only configuration files.
* TODOs
** Sudoers
   using ~security.sudo~
** Sway config
   Migrate from the i3 config files, i3 status bar, dunst, etc.
** Create the =default= and =work= ssh keys
   Ideally do this in home manager automatically unless the files already exist.
** Clone this repo
** Rename the VM hostname to =qemu=
   Need to split the config up so the hostname in configuration.nix isn't the same on
   all installations. I don't yet know how to set the computer's hostname predictably
   during installation.
** Migrate Emacs
   Jesus Fucking Christ, that'll be a nightmare.
